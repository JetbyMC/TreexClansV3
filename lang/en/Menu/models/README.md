- This folder contains the model menus.

Models are mandatory menus without which the plugin cannot function.

YOU CAN SAFELY DUPLICATE IF:

- You want to create multiple GUIs with different configurations but the same model — create the model in the custom
  folder, because only one model is loaded from the models folder.

You can configure these models however you like, as long as each model exists, even if it is not used anywhere.